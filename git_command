# Git Global Config
git config --global user.name "Rotanak Chhean"
git config --global user.email rotanakchhean@gmail.com
git config --global -e
git config --global core.editor "code --wait"
git config --global core.autocrlf true ("true" in window, "input" in linux/mac)

# Handle project
git init
git add . ("." all files in the directory, or "filename.extension)


#or create a new repository on the command line
echo "# robotinvest" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/mrrotanak/robotinvest.git
git push -u origin main

#remove file or folder from git track
git rm --cached file_name
git rm -r --cached folder_name
